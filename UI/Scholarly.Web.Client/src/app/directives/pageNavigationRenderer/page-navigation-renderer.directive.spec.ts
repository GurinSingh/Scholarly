import { PageNavigationRendererDirective } from './page-navigation-renderer.directive';

describe('PageNavigationRendererDirective', () => {
  it('should create an instance', () => {
    const directive = new PageNavigationRendererDirective();
    expect(directive).toBeTruthy();
  });
});
