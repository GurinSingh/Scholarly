import { ArticleInfoPopoverDirective } from './article-info-popover.directive';

describe('ArticleInfoPopoverDirective', () => {
  it('should create an instance', () => {
    const directive = new ArticleInfoPopoverDirective();
    expect(directive).toBeTruthy();
  });
});
