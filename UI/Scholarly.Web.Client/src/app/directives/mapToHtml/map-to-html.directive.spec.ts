import { MapToHtmlDirective } from './map-to-html.directive';

describe('MapToHtmlDirective', () => {
  it('should create an instance', () => {
    const directive = new MapToHtmlDirective();
    expect(directive).toBeTruthy();
  });
});
